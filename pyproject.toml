[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "awewarm"
dynamic = ["version"]
description = "Keep AI coding-plan subscription windows warm with minimal scheduled requests."
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["click>=8.1", "tzdata; platform_system == 'Windows'"]
authors = [
  { name = "Peng" }
]
license = "MPL-2.0"
keywords = ["ai", "agent", "claude", "codex", "cli", "scheduler", "keep-alive"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
  "Topic :: Utilities"
]

[project.urls]
Homepage = "https://github.com/wehuman01/awewarm"
Repository = "https://github.com/wehuman01/awewarm"
Changelog = "https://github.com/wehuman01/awewarm/blob/main/docs/CHANGELOG.md"
Hub = "https://github.com/wehuman01/awewarm-hub"

[project.scripts]
awewarm = "awewarm.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.dynamic]
version = {attr = "awewarm.__version__"}
