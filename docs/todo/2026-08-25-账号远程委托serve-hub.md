# TODO: 账号（CLI 登录）远程委托到 serve / hub

日期：2026-08-25
状态：已在 feature/delegate-cli-accounts 分支实现（v0.6.3，2026-08-26 发布；后续
原生 HTTP 传输解掉了“服务器必须装 CLI”的前提，见
docs/superpowers/specs/2026-08-26-native-account-transport-design.md）。
设计稿：docs/superpowers/specs/2026-08-25-account-delegation-design.md。

实现说明：
- credstore.py（新）读取本地登录 + sha256 指纹（16 hex）；
- claude 走 CLAUDE_CODE_OAUTH_TOKEN，codex 走 CODEX_HOME 沙箱（每次触发前用推送的
  凭据重写 auth.json，服务器侧刷新一律丢弃）；
- 服务端 tick 线程化（4 worker 池 + 每连接互斥），激活不再持全局锁；
  put_connection 放开账号 kind、服务端解析 CLI 路径、落盘指纹；
- 本地侧确认门（点名服务器 URL）、漂移重推、run 的 CLI 客户端超时 135 s；
- 测试：test_credstore（新）+ transport/server/config/cli/persist_key/remote 增补。

遗留：
- claude 的 CLAUDE_CODE_OAUTH_TOKEN 验证为弱正向：本机带 garbage token 的请求确实
  发出（未回退本地登录，CLI 把环境变量当作请求凭据），但被 ~/.claude 的第三方
  网关污染返回 500，干净的 401 验证仍待无网关环境；spec 中的 CLAUDE_CONFIG_DIR
  兜底路径未实现。
- awewarm-hub（独立仓库）：信任文案提到登录凭据、测试、版本 pin 需协同更新。
- 真机端到端（2026-08-26，本机 codex 账号 + 本地 serve v0.7.0）：委托→服务器实发
  ok（13.8 s）→沙箱 auth.json 与本地登录一致（0600）→服务器磁盘无秘密→重启后
  remote push 重推凭证→--local 收回清掉沙箱，全部通过。

## 目标

Claude Code / Codex 登录账号（claude-cli / codex-cli 传输）可以像 API key 订阅一样
委托给 `awewarm serve` / `awewarm-hub serve` 保温。设计稿（已写好，含完整方案）：

    docs/superpowers/specs/2026-08-25-account-delegation-design.md

核心模型：本地读取登录凭证 → 像 API key 一样推送到服务器 RAM → 服务器以环境变量
注入跑 CLI（claude 用 `CLAUDE_CODE_OAUTH_TOKEN`，codex 用 `CODEX_HOME` 沙箱）。
本地登录永远是事实源，服务器侧刷新结果丢弃，指纹不一致时自动重推。

## 已完成的验证（spike，2026-08-25 本机实测）

- ✅ Keychain `Claude Code-credentials` 用 `security find-generic-password -w`
  可无交互读取（只验证了可读性，未记录内容）。
- ✅ codex 尊重 `CODEX_HOME`：空目录下 `codex exec` 401 无凭证报错，
  且会话状态文件写进沙箱目录（v0.149.0 实测）。
- ❓ claude `CLAUDE_CODE_OAUTH_TOKEN` **未验证完**：本机 `~/.claude/settings.json`
  配了第三方网关（ANTHROPIC_BASE_URL=api.42w.shop + kimi-k3 模型映射），
  污染了测试。重试方法：
  `echo '{}' > /tmp/s.json && CLAUDE_CODE_OAUTH_TOKEN=garbage claude -p --settings /tmp/s.json --model haiku "Reply with exactly: ok"`
  预期 401/invalid token = 环境变量被尊重；若走本地登录成功 = 需要改用
  `CLAUDE_CONFIG_DIR` 沙箱兜底方案（spec 已写明兜底路径）。

## 剩余工作（按 spec 分期）

1. stage 0 收尾：验证 claude 环境变量（上面的命令）。
2. stage 1 服务端：`credstore.py`（新）、`transport.py` 环境注入、`server.py`
   （put_connection 放开账号 + 服务器侧 CLI 路径解析、指纹存储、tick 线程化
   ＋按连接互斥、codex 沙箱生命周期）。
3. stage 2 本地：`cli.py` `_delegate_remote` 账号路径＋确认门、`_require_secret`、
   `_sync_remote` 指纹漂移重推、`remote.py` 推送体加 fingerprint ＋ CLI 的 run
   超时放大、`config.py` `connection_errors` 删掉 remote+account 禁令。
4. 测试：unittest（test_transport / test_server / test_config / test_cli）。
5. 文档：README.md、README_cn.md、docs/CHANGELOG.md、CONTRIBUTING 的
   "Remote delegation is single-owner (subscription only)" 约束条目需随 spec 更新。
6. 分支 `feature/delegate-cli-accounts`（从 dev=b7c2d41 建过，零提交，已删），
   重做时重新建分支，完成后 `gh pr create --base dev`，再切回 dev。

## 注意

- 工作树里有用户自己的未提交文件（docs/media/0825/…、docs/todo/…），不要带进提交。
- 账号凭证是账户级秘密，比 API key 更敏感：委托要有显式确认门，hub 信任文案要加重。
